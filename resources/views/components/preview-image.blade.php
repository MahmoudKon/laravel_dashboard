<div class="media">
    <span class="avatar avatar-md">
        <img src="{{ $image }}" loading="lazy" alt="{{ $alt }}" class="cursor-pointer preview-modal-image rounded {{ $classess }}" data-toggle="tooltip" title="{{ $title }}" width="{{ $width }}">
    </span>
</div>
